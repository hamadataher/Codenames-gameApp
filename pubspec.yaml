name: codenames_bgu
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.2
  firebase_core: ^3.7.0
  cupertino_icons: ^1.0.8
  # Use flutter_launcher_icons instead, which supports null safety
  flutter_launcher_icons: ^0.14.1
  youtube_player_flutter: ^9.1.1
  firebase_dynamic_links: ^6.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/designer.jpeg
    - assets/images/backgroundd.jpg
